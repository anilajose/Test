general:
  branches:
    only:
        - /QA_.*/
 
machine:
 
  java:
    version: openjdk7  

dependencies:
  pre:
	
    - npm install -g jasmine
test:
  override:
   
    - 'cd src && gradle check jacocoTestReport'
post:
  
    - cp -R build/reports/* $CIRCLE_ARTIFACTS    
